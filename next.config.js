/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  swcMinify: true,
};

module.exports = {
  images: {
    domains: ["fakestoreapi.com", "datagma.com"],
  },
  reactStrictMode: true,
  swcMinify: true,
};
